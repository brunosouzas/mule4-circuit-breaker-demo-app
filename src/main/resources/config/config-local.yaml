# No overrides for local: common.yaml already has everything this environment needs. This file only
# has to exist because global.xml resolves config/config-${mule.env}.yaml and "local" is the default
# mule.env.
